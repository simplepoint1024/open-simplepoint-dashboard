package org.simplepoint.plugin.notification.api.model;

/** Change types sent to connected notification clients. */
public enum NotificationChangeType {
  PUBLISHED,
  REVOKED,
  USER_STATE_CHANGED
}
